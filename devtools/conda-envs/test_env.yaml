<<<<<<< HEAD
name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
=======
name: basicrta-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
>>>>>>> basicrta-kit/master
  #- pip:
  #  - codecov

